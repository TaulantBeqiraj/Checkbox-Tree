import React from "react";

const Summary = () => {
  return (
    <section className="summary-container" style={{ marginTop: "25px" }}>
        <p>Selected:</p>
        <button type="submit" className="btn">
          Summary
        </button>
    </section>
  );
};

export default Summary;
